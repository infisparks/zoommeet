version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: zoomeet_coolify
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - PORT=5000
      - LIVEKIT_URL=wss://live.infiplus.in
      - LIVEKIT_API_KEY=infiplus_livekit
      - LIVEKIT_API_SECRET=Lk9mQ2xV7rT4pN8aK5zH1wC6dF3yB9eJ
      - NEXT_PUBLIC_BACKEND_URL=https://meets.infiplus.in
